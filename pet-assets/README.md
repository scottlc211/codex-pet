# Pet Assets

这个目录只用于仓库内的资源示例，不再加入桌面应用的本地文件访问范围。

使用设置页导入 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`、`.svg`、`.apng`，或带 `pet.json` / `theme.json` 的动画包。应用会把允许的文件复制到 `~/.codex-pet/pets/`，刷新后即可发现。选中主题后，可为每种桌宠状态预览并选择主题内已有动作；图集必须符合 8 列 × 9 行格式。
